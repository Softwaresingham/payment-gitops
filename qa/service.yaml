apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: qa

  labels:
    app: payment-service
    environment: qa

spec:

  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
