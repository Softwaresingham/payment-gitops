apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: dev

  labels:
    app: payment-service
    environment: dev

spec:

  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
