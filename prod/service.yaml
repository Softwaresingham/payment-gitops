apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: prod

  labels:
    app: payment-service
    environment: prod

spec:

  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
